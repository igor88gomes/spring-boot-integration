# Exempel på miljövariabler (.env.example)
# Används endast för lokal utveckling/test.
# Lägg RIKTIGA värden i din lokala fil `.env` som inte checkas in (finns i .gitignore).

# Databas
DB_HOST=postgres
DB_PORT=5432
DB_NAME=integrationdb
DB_USER=integration
DB_PASSWORD=integration_pass

# ActiveMQ
BROKER_URL=tcp://activemq:61616
BROKER_USER=admin
BROKER_PASS=admin